{
  "private": true,
  "name": "sysinfo",
  "scripts": {
    "build": "cat /proc/cpuinfo && echo 'cgroup info -------' && cat /proc/1/cgroup && ps aux && node sysinfo.js"
  },
  "dependencies": {
    "systeminformation": "5.21.17"
  },
  "devDependencies": {
    "wrangler": "^4.45.3"
  }
}
